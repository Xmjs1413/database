import * as React from 'react';
import { Divider } from 'antd';

const WrapperDivider = (props: any) => {
    return <Divider style={{ paddingBottom: '12px', backgroundColor: '#fff', margin: '0px' }} />;
};

export default WrapperDivider;
